const Authentication = () => {
  //check if user is loggedin in localstorage
  // if logged in then set usestate to logged in

  //check if usestate is logged in
  // if logged in show portfolio
  // else show this component

  return <div>hello not auth</div>;
};

export default Authentication;
